# TrueWebsiteCloner V1.1 Roadmap

V1.1 extends the V1.0 capture model while keeping sensitive authentication material disabled by default and preserving deterministic gates.

## Gate 1.1 — WebSocket capture

Status: merged to `main`; Gate 1.1 and the V1.0 regression/aggregate sweep pass.

Capture same-origin WebSocket lifecycle and bounded frame payloads through Chrome DevTools Protocol. Maximum stored frame payload: 64 KiB. Handshake headers, cookies and Authorization remain excluded.

## Gate 1.2 — XHR / Fetch request payloads

Status: merged to `main`; Core, real Chrome and final aggregate gates pass.

Capture bounded request payloads for same-origin XHR/Fetch requests with explicit content-type rules and a 64 KiB limit. Supported JSON and form payloads are parsed by Core and sensitive values are redacted before persistence. Unsupported, unavailable or oversized payloads use metadata-only fallback. Raw HTTP headers, cookies and Authorization remain excluded from this capture path.

## Gate 1.3 — Safe HTTP headers

Status: implementation branch.

Capture only exact allow-listed request/response headers for same-origin traffic. The extension filters before forwarding and Core independently enforces the same allowlists, per-value/aggregate size limits, CR/LF rejection and explicit exclusion of authentication, cookie, API-key and token headers. Raw header blocks are never persisted.

## Gate 1.4 — Deterministic API replay fixtures

Planned.

Use captured same-origin request/response fixtures to improve local replay matching by method, normalized URL and bounded redacted request payload. Authentication replay is not enabled by default. Replay must remain local and deterministic.

## V1.1 completion rule

V1.1 is not releasable until every new gate is green together with the existing V1.0 regression/release-candidate sweep.
